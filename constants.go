package smartcar

// Connect URL constants
const (
	ConnectScheme = "https"
	ConnectHost   = "connect.smartcar.com"
	ConnectPath   = "oauth/authorize"
)

// Exchange URL constant
const ExchangeURL = "https://auth.smartcar.com/oauth/token"
